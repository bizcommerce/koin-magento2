<?php
/**
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category    Koin
 * @package     Koin_Payment
 *
 */

/** @var \Koin\Payment\ViewModel\Product\BnplBanner $viewModel */
$viewModel = $block->getViewModel();
?>

<?php if ($viewModel->isEnabled()): ?>
<div class="koin-bnpl-banner"
     data-mage-init='{"koinBnplBanner": <?= $escaper->escapeHtmlAttr($viewModel->getJsonConfig()) ?>}'
     data-product-id="<?= $escaper->escapeHtmlAttr($viewModel->getProductId()) ?>"
     data-form-key="<?= $escaper->escapeHtmlAttr($viewModel->getFormKey()) ?>">
    <button type="button"
            class="koin-bnpl-button action primary"
            id="koin-bnpl-banner-btn"
            data-original-text="<?= $escaper->escapeHtmlAttr($viewModel->getBannerText()) ?>">
        <span class="koin-text"><?= $escaper->escapeHtml($viewModel->getBannerText()) ?> <img src="<?= $escaper->escapeUrl($viewModel->getLogoUrl()) ?>"
                                                                                              alt="Koin"
                                                                                              class="koin-logo"/></span>
    </button>
</div>
<?php endif; ?>
